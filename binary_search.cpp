#include<iostream>
using namespace std;
int main()
{
   int a[100];
   int n,b;
   cout << "Enter array elements:"
   cin >> n;
   cout << n;
   cout << "Array elements are:";
   for(int i=0;i<n;i++)
   {
    cin >> a[i];
   }
   if(n%2==0)
    {
        mid=n/2;
    }
    else{
        mid=(n+1)/2;
    }
    cout << "Enter number to be found:"
    cin >> b;
    a[0]=start;
    a[n-1]=end;
    for(int i=o;i<n;i++)
    {
        if(b>a[mid])
        {
            for()
        }
    }
    
}